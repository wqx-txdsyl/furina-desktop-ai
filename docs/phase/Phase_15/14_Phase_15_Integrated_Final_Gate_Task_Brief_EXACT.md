# Phase 15 — Cognitive Life Integrated Final Gate
# EXACT TASK BRIEF

Repository:
`wqx-txdsyl/furina-desktop-ai`

Phase 15 integration branch:
`feature/phase15-cognitive-life-finalization`

Phase 14 code frozen SHA:
`f8e84ecc7be67fbfa9d78f00b056bce4dd420095`

Phase 15 documentation baseline:
`4442fac4de1deabaf967d2f029032f0076512ab7`

Phase 15 Master Plan adoption commit reported by operator:
`a5bd81c`

External reviewer authority:
`GPT-5.6 Sol`

Permanent constraints:

```text
C1-C7 authority boundaries remain frozen.
Derived retrieval is DERIVED / REBUILDABLE / NON-AUTHORITATIVE / NOT C8.
No coding agent may declare PHASE_15_FINAL_GATE_PASS.
Each bounded task must stop at READY_FOR_REVIEW.
No task may absorb later-phase Character Agency / Hermes / GUI / Voice scope.
No skip / xfail / weakened assertion / fabricated Canon evidence.
```


Document path:

`docs/phase/Phase_15/14_Phase_15_Integrated_Final_Gate_Task_Brief_EXACT.md`

Recommended branch:

`feature/phase15-integrated-final-gate`

Base:

latest integration SHA after D1, D4, D2, D3, D5 each received independent reviewer PASS.

---

# 1. PURPOSE

This task adds no planned feature.

It exists to prove the entire Phase 15 cognitive system works together under real production paths, restart, failure, and authority conflicts.

Allowed work:

```text
integration tests
static audits
small blocker fixes discovered by the gate
documentation
```

Any blocker fix must be:
- minimal;
- directly caused by integration failure;
- documented.

No opportunistic feature work.

---

# 2. FINAL INVARIANTS

Prove simultaneously:

```text
C1 identity stable
C2 Canon provenance truthful
C3 MemoryEngine formation authority
C4 explicit truth/lifecycle/provenance/temporal semantics
C5 relationship truth/provenance/anti-spam
C6 append-only objective truth
C7 verified task truth
derived retrieval non-authoritative
no C8
```

---

# 3. INTEGRATED SCENARIO I1 — TEMPORAL PLAN

Real production direct turn:

```text
user: explicit relative-time plan
→ reserve DirectTurn
→ canonical USER_MESSAGE U
→ deterministic interpretation
→ resolved temporal semantics
→ C4 PLAN row
→ exact source provenance
→ context assembly
→ restart
→ same semantic date after restart
```

Force U persistence failure once:

```text
no C4 mutation
dialogue terminal behavior remains safe
```

---

# 4. I2 — CURRENT TRUTH VS STALE DERIVED INDEX

Create:

```text
old preference
→ index built
→ user explicitly corrects preference
→ old row superseded
→ deliberately leave stale derived representation
→ retrieval query
```

Required:

```text
authoritative active C4 truth wins
```

Then rebuild index and verify stale representation disappears.

---

# 5. I3 — HYBRID RETRIEVAL

Prove:

```text
exact lexical query
semantic paraphrase
temporal query
embedding unavailable
corrupted vector index
deleted index
rebuild
```

all remain safe.

No vector-only false truth.

---

# 6. I4 — EXPOSURE CONTROL

```text
C3 memory automatically injected
→ adjacent unrelated turn
→ exposure penalty
→ explicit user recall
→ bypass
→ memory returns
```

No C3 mutation.

---

# 7. I5 — RELATIONSHIP BURST

Use real interaction/event production paths where practical.

```text
positive burst
negative burst
spaced long-term interactions
```

Required:

```text
bounded delta
C6 event truth intact
milestone provenance intact
```

---

# 8. I6 — CANON

If D1 verified Acts II/III:

prove only officially verified coverage improved.

If not:

prove PARTIAL state remains truthful.

Either outcome may PASS.

---

# 9. I7 — RESTART

Persist state, stop, recreate runtime.

Prove:

```text
C1-C7 durable truth preserved
C4 temporal semantics preserved
C5 durable state preserved
derived index reload/rebuild safe
exposure cache follows documented persistence semantics
no duplicate consolidation
no duplicate transition
```

---

# 10. I8 — AUTHORITY ATTACK MATRIX

Create explicit counterexamples:

```text
vector says stale thing vs C4 active truth
old C3 vs current user statement
community Canon locator vs official C2
Agent FAILED vs "success-like" memory text
relationship internal state vs visible UI assumptions
```

Every conflict must resolve according to frozen authority ladder.

---

# 11. I9 — STATIC WRITER AUDIT

Repo-wide audit for durable writes.

At minimum identify all writers to:

```text
C2 data
C3 memories
C4 user_model_items
C5 relationship state/milestones
C6 events
C7 tasks
derived index
```

Verify no unauthorized second writer emerged.

---

# 12. I10 — PERFORMANCE / LOOP DISCIPLINE

Verify:

```text
no DB/vector work in 60fps renderer loop
no per-frame embedding
no unbounded context
no unbounded event scan per turn
```

Run reasonable cognition stress/restart cycles.

---

# 13. TEST GATES

Required:

```text
Gate A integrated final tests
Gate B all D1 tests
Gate C all D4 tests
Gate D all D2 tests
Gate E all D3 tests
Gate F all D5 tests
Gate G Phase14 reviewer preservation
Gate H Agent/Office foundation
Gate I full suite ×3 consecutive
```

Full suite requirements:

```text
0 failed
0 task-introduced skipped
0 task-introduced xfailed
```

Do not retry until lucky.

Record exact counts and durations for all three.

---

# 14. FINAL STATIC AUDIT

Answer YES/NO with evidence:

```text
A  Is C1-C7 still exactly seven authoritative stores?
B  Is derived retrieval still non-authoritative?
C  Can index deletion lose truth?
D  Can stale C4 derived text override active C4?
E  Can C3 form without valid source provenance?
F  Can direct C4 mutate without canonical U?
G  Can C2 runtime mutate?
H  Can community locator count as official Canon?
I  Can rapid interactions farm C5 without bound?
J  Can cooldown hide explicit recall?
K  Can Agent FAILED become verified C7 success?
L  Did any Phase17 proactive behavior enter Phase15?
M  Did any test get weakened/skipped/xfail?
N  Does restart preserve durable truth?
O  Does local SHA equal remote SHA?
```

Required:

```text
A YES
B YES
C NO
D NO
E NO
F NO
G NO
H NO
I NO
J NO
K NO
L NO
M NO
N YES
O YES
```

---

# 15. DOCUMENTATION

Create:

`docs/phase/Phase_15/15_Phase_15_Integrated_Final_Closeout_Report_EXACT.md`

Update:

`docs/phase/Phase_15/00_MANIFEST.md`

Do not create Phase 16 docs in this task.

---

# 16. FINAL STATUS

Coding agent may output only:

```text
READY_FOR_FINAL_REVIEW
```

or exact blocker.

Only external reviewer can issue:

```text
PHASE_15_FINAL_GATE = PASS
```
